<!-- components/text-time/text-time.wxml -->
<view class="reports-form-box">
  <!-- 标题 -->
  <view class="form-inputname">
    {{forminfo.label}}
    <text wx:if="{{forminfo.force}}" class="force-text">*</text>
  </view>
  <!-- 描述 -->
  <view wx:if="{{forminfo.text.length>0}}" class="form-describe mt-10">{{forminfo.text}}</view>
  <!-- 主体 -->
  <checkbox-group bindchange="checkboxChange">
    <!-- 选项 -->
    <label class="radio-group">
      <block wx:for="{{forminfo.data}}">
        <checkbox
          disabled="{{forminfo.limit && item.limit <= 0}}"
          value="{{item.name}},{{item.id}},{{item.duration}}"
          wx:key="id"
        >
          <view class="radio-item">
            {{item.name}}
            <text wx:if="{{forminfo.limit}}" class="form-describe ml-20">剩余{{item.limit}}</text>
            <text wx:if="{{forminfo.duration}}" class="form-describe ml-20">时长{{item.duration}}</text>
          </view>
        </checkbox>
      </block>
    </label>
  </checkbox-group>
</view>
